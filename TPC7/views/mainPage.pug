extends layout

block content
  div(class="container")
    table
      tr
        th Código
        th Título
      each a in dataClass1
        tr 
          td
            a(href='/' + a.codigo)=a.codigo
          td= a.titulo